<UserControl x:Class="UsersAD"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Hospital_Ultimate"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      FontFamily="Noto Sans">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="AdminsUsers"/>
        <materialDesign:Card Background="{DynamicResource PrimaryHueMidBrush}" Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}" Grid.Column="1" Padding="8">
            <TextBlock
                FontSize="16" FontWeight="Light" HorizontalAlignment="Center">
                Agregar o Modificar Usuarios
            </TextBlock>
        </materialDesign:Card>
        <TextBox x:Name="Txt_newUser"
                 Grid.Column="1"
                Margin="60 60 60 0"
                materialDesign:HintAssist.Hint="Nombre del nuevo usuario"
                Text="{Binding Name}"
                 Foreground="#FF1976D2"
                VerticalAlignment="Top" />
    </Grid>
</UserControl>
